query ClientRelatives(
    $clientId: ID!,
    $relation: RelationType
) {
    clientRelatives(clientId: $clientId, relation: $relation) {
        id
        relationType
        name
        age
    }
}